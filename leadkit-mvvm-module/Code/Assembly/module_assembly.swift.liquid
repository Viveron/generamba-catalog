{% include 'header' %}

import UIKit

final class {{ prefix }}{{ module_info.name }}ModuleAssembly {
    
    static func createModule() -> {{ prefix }}{{ module_info.name }}Module {
        let viewModel = {{ prefix }}{{ module_info.name }}ViewModel()
        let view = {{ prefix }}{{ module_info.name }}ViewController(viewModel)
        
        viewModel.view = view
        
        return view
    }
}
