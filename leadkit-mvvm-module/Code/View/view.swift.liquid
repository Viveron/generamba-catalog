{% include 'header' %}

import UIKit

final class {{ prefix }}{{ module_info.name }}View: BaseView {
    
    // MARK: - Life cycle
    
    override func configureAppearance() {
        super.configureAppearance()
        
    }
    
    override  func configureLayout() {
        super.configureLayout()
        
    }

    // MARK: - Private methods

}