{% include 'header' %}

import UIKit
import LeadKit

final class {{ prefix }}{{ module_info.name }}ViewController: ViewController<{{ prefix }}{{ module_info.name }}ViewOutput, {{ prefix }}{{ module_info.name }}View>, {{ prefix }}{{ module_info.name }}Module {

	// MARK: - {{ prefix }}{{ module_info.name }}Module

	var onClose: VoidBlock?

    // MARK: - Life cycle
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
        viewModel.viewDidLoad()
    }
    
    override func configureAppearance() {
        super.configureAppearance()
        
    }
    
    // MARK: - Private methods
    
}

// MARK: - {{ prefix }}{{ module_info.name }}ViewInput
extension {{ prefix }}{{ module_info.name }}ViewController: {{ prefix }}{{ module_info.name }}ViewInput {
    
}
